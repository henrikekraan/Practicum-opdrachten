#include <studio.h>

int main()
{
    int x, y, z;
    
    printf("Typ twee cijfers: \n");
    scanf("%d%d",&a,&b);
    
    z = x + y;
    
    printf("%d + %d = %d|n", x, y, z);
    
    return 0;
}